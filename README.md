# Toy Calculator

A tiny, intentionally imperfect calculator, used as a practice repo for a
GitHub-issue-to-PR agent project. Do not use in production. :)

## Known issue
See the Issues tab -- `divide()` crashes with an unhandled `ZeroDivisionError`
instead of raising a clear, catchable error.
